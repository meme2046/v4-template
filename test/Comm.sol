
contract Comm{
    // 查看ERC20代币余额
    // uint256 balance0 = IERC20(address(currency0)).balanceOf(address(this));
    // uint256 balance1 = IERC20(address(currency1)).balanceOf(address(this));

    // 或者使用Currency库的方法
    // uint256 balance0 = CurrencyLibrary.toUint256(currency0.balanceOf(address(this)));
    // uint256 balance1 = CurrencyLibrary.toUint256(currency1.balanceOf(address(this)));
}
